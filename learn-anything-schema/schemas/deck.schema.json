{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://learn-anything.dev/schemas/deck.json",
  "title": "SRS Deck",
  "description": "Schema for srs/deck.json — spaced repetition card deck",
  "type": "object",
  "required": ["cards"],
  "properties": {
    "cards": {
      "type": "object",
      "description": "Card ID → Card mapping. Keys are '{courseId}-{moduleId}-{questionId}'",
      "additionalProperties": { "$ref": "card.json" }
    }
  }
}
